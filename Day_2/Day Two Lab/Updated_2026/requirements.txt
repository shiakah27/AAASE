# Day 2 Lab — Enterprise Autonomous Research AI Agent
# Install:  pip install -r requirements.txt
# Python 3.10+

# --- Core (required) ---
langgraph>=1.0            # StateGraph, START/END, checkpointers
langchain>=1.0            # core abstractions, messages
langchain-openai>=1.0     # ChatOpenAI + OpenAIEmbeddings (also works
                          # with Groq/Gemini via base_url)
langchain-tavily>=0.2     # TavilySearch web search tool
                          # (replaces deprecated TavilySearchResults)
python-dotenv>=1.0        # load .env API keys

# --- Optional: persistent vector memory ---
# Heavy install (can take several minutes). Without it the lab
# falls back to LangChain's built-in InMemoryVectorStore.
# langchain-chroma>=0.2

# --- Optional: local embeddings (no OpenAI key needed) ---
# langchain-huggingface
# sentence-transformers

# API keys go in a .env file (see README.md):
#   OPENAI_API_KEY=...      (or a Groq/Gemini key + LLM_BASE_URL)
#   TAVILY_API_KEY=tvly-... (free: 1,000 credits/month at tavily.com)
# Or skip keys entirely:  USE_FAKE=1
